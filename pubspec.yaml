name: vaishnanva_calendar
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=2.19.6 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_redux: ^0.10.0
  flutter_svg: ^2.0.5
  get: ^4.6.5
  google_fonts: ^4.0.4
  redux: ^5.0.0
  redux_thunk: ^0.4.0
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
